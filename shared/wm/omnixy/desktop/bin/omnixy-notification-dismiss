#!/bin/bash

# omnixy:summary=Dismiss a notification by summary substring. Used by the first-run notifications to dismiss them after clicking for action.
# omnixy:args=<summary>

if (($# == 0)); then
  echo "Usage: omnixy-notification-dismiss <summary>"
  exit 1
fi

omnixy-shell -q notifications dismiss "$1"
