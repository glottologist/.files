#!/bin/bash

# omnixy:summary=Show which theme is styling the Plymouth boot screen
# omnixy:examples=omnixy plymouth current

# omnixy-plymouth-set installs the theme's unlock.png as a byte-for-byte
# copy of logo.png, so matching it back identifies the current theme.
installed_logo=/usr/share/plymouth/themes/omnixy/logo.png

if cmp -s "$OMNIXY_PATH/default/plymouth/logo.png" "$installed_logo"; then
  echo default
  exit
fi

omnixy-plymouth-list | while read -r name; do
  if cmp -s "$(omnixy-theme-dir "$name")/unlock.png" "$installed_logo"; then
    echo "$name"
    exit
  fi
done
