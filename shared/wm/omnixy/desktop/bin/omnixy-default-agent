#!/bin/bash

# omnixy:summary=Set and launch the default coding agent
# omnixy:args=[pi|omp|opencode|claude|codex|grok|gemini|copilot|crush]
# omnixy:examples=omnixy default agent | omnixy default agent codex | omnixy default agent claude

installing=false
if [[ ${1:-} == "--install" ]]; then
  installing=true
  shift
fi

agent_file="$HOME/.config/omnixy/defaults/agent"

if (($# == 0)); then
  if [[ -f $agent_file ]]; then
    read -r agent <"$agent_file"
  fi

  # Silent when unset rather than defaulting: Omnixy picks no agent for you, so
  # the menu leaves every entry unchecked until one is chosen.
  [[ -n ${agent:-} ]] && echo "$agent"
  exit 0
fi

case "$1" in
pi) agent="pi"; name="Pi" ;;
omp | oh-my-pi) agent="omp"; name="Oh My Pi"; agent_package="github:can1357/oh-my-pi" ;;
opencode | open-code) agent="opencode"; name="OpenCode" ;;
claude | claude-code) agent="claude"; name="Claude Code" ;;
codex) agent="codex"; name="Codex" ;;
crush) agent="crush"; name="Crush" ;;
grok) agent="grok"; name="Grok"; agent_package="npm:@xai-official/grok" ;;
gemini | gemini-cli) agent="gemini"; name="Gemini" ;;
copilot | github-copilot) agent="copilot"; name="GitHub Copilot" ;;
*)
  echo "Usage: omnixy-default-agent <pi|omp|opencode|claude|codex|grok|gemini|copilot|crush>"
  exit 1
  ;;
esac

agent_package=${agent_package:-$agent}

if [[ $installing == "false" ]] && ! mise where "$agent_package" &>/dev/null; then
  exec omnixy-launch-floating-terminal-with-presentation omnixy-default-agent --install "$agent"
fi

if ! mise use -g "$agent_package"; then
  if [[ $installing == "true" ]]; then
    echo "Could not install $name with mise" >&2
  else
    echo "Could not set $name as the default coding agent" >&2
  fi
  exit 1
fi

mkdir -p "$(dirname "$agent_file")"
printf '%s\n' "$agent" >"$agent_file"

if [[ $installing == "true" ]]; then
  printf '\033[2J\033[3J\033[H'
  exec omnixy-agent --inline
else
  exec omnixy-agent
fi
