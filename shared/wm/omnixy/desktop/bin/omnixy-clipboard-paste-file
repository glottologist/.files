#!/bin/bash

# omnixy:summary=Copy a file to the clipboard and paste it
# omnixy:group=clipboard
# omnixy:args=[--copy-only] <mime-type> <path>
# omnixy:examples=omnixy clipboard paste file image/png /tmp/screenshot.png
# omnixy:hidden=true

copy_only=false

if [[ ${1:-} == "--copy-only" ]]; then
  copy_only=true
  shift
fi

mime=${1:-}
path=${2:-}

if [[ -z $mime || -z $path ]]; then
  echo "Usage: omnixy-clipboard-paste-file [--copy-only] <mime-type> <path>" >&2
  exit 1
fi

[[ -r $path ]] || exit 1

wl-copy --type "$mime" < "$path"

if [[ $copy_only == "true" ]]; then
  exit
fi

sleep 0.15
wtype -M shift -k Insert -m shift 2>/dev/null || true
