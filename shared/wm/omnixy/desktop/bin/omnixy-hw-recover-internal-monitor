#!/bin/bash

# omnixy:summary=Clear the internal-monitor-disable toggle if no external display is connected.

TOGGLE="$HOME/.local/state/omnixy/toggles/hypr/internal-monitor-disable.lua"

if [[ -f $TOGGLE ]] && ! omnixy-hw-external-monitors; then
  rm -f "$TOGGLE"
fi
